# ───────────────── Puertos y URL base ─────────────────
PORT=3005                        # Puerto en el que se levanta Express
PUBLIC_BASE_URL=http://0.0.0.0:3005
                                   # URL pública que servirá los ficheros /media

# ──────────────── Integración API externa (opcional) ────────────────
EXTERNAL_API_ENABLED=false        # true | false  → activa las llamadas HTTP externas
EXTERNAL_API_URL=https://midominio.com/whatsapp/webhook
EXTERNAL_API_TOKEN=<TU_TOKEN_SEGURO>
                                   # Se envía en el payload como “token”

# ──────────────── Playwright (opcional, solo si usas navegadores) ────────────────
# PLAYWRIGHT_BROWSERS_PATH=/var/www/.cache/ms-playwright
                                   # Ya se exporta en el script; sólo descomenta
                                   # si quieres cambiar la ubicación

# ───────────────── Otros (añade según necesidad) ─────────────────
# NODE_ENV=production              # production | development
# LOG_LEVEL=debug                  # trace | debug | info | warn | error
